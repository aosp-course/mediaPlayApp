<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/car"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundPrimary"
    tools:context=".ui.car.CarFragment">

    <ImageView
        android:id="@+id/backButton"
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:src="@drawable/back_button"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="30dp"
        android:layout_marginStart="35dp"
        />

    <!-- Botão de ler velocidade -->
    <Button
        android:id="@+id/btnReadSpeed"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Ler Velocidade"
        android:background="@drawable/button_car_style"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:padding="64dp"
        app:layout_constraintHorizontal_chainStyle="packed"
        app:layout_constraintEnd_toStartOf="@+id/btnSendCanVolume"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintHorizontal_bias="0.5"
        app:layout_constraintVertical_bias="0.4" />

    <!-- Botão para enviar sinal CAN de volume -->
    <Button
        android:id="@+id/btnSendCanVolume"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Enviar Sinal CAN de Volume"
        android:background="@drawable/button_car_style"
        android:textColor="@android:color/white"
        android:textSize="20sp"
        android:padding="64dp"
        android:layout_marginStart="16dp"
        app:layout_constraintStart_toEndOf="@+id/btnReadSpeed"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/btnReadSpeed"
        app:layout_constraintBottom_toBottomOf="@+id/btnReadSpeed" />

    <!-- LinearLayout para agrupar o label e o valor da velocidade -->
    <LinearLayout
        android:id="@+id/llSpeedDisplay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/btnReadSpeed"
        app:layout_constraintStart_toStartOf="@+id/btnReadSpeed"
        app:layout_constraintEnd_toEndOf="@+id/btnReadSpeed"
        >
        <!-- TextView para o label da velocidade -->
        <TextView
            android:id="@+id/tvSpeedLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Velocidade:"
            android:textSize="20sp"
            android:textColor="@android:color/white" />

        <!-- TextView para o valor da velocidade -->
        <TextView
            android:id="@+id/tvSpeedValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="-- km/h"
        android:textSize="20sp"
        android:textColor="@android:color/white"
        android:layout_marginStart="8dp" />
    </LinearLayout>

    <!-- LinearLayout para agrupar o label e o valor do volume CAN -->
    <LinearLayout
        android:id="@+id/llCanVolumeDisplay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginTop="16dp"
        app:layout_constraintTop_toBottomOf="@+id/btnSendCanVolume"
        app:layout_constraintStart_toStartOf="@+id/btnSendCanVolume"
        app:layout_constraintEnd_toEndOf="@+id/btnSendCanVolume"
        >
        <!-- TextView para o label do volume CAN -->
        <TextView
            android:id="@+id/tvCanVolumeLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Volume CAN:"
            android:textSize="20sp"
            android:textColor="@android:color/white" />

        <!-- TextView para o valor do volume CAN -->
        <TextView
            android:id="@+id/tvCanVolumeValue"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="--"
        android:textSize="20sp"
        android:textColor="@android:color/white"
        android:layout_marginStart="8dp" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
